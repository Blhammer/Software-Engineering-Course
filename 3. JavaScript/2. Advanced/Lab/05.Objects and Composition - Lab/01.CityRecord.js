function solve(name, population, treasury) {
    const record = {};

    record.name = name;
    record.population = population;
    record.treasury = treasury;

    return record;
}
solve('Tortuga',
    7000,
    15000
);
solve('Santo Domingo',
    12000,
    23500
);